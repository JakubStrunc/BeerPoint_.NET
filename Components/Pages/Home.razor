@page "/"
@using PNET_semestralka_blazor_app.Controllers
@using PNET_semestralka_blazor_app.Models
@inject HomeController HomeService
@inject Session session

<PageTitle>Home</PageTitle>


@if (session.UserEmail != null)
{
    <p>Welcome, @session.UserEmail! (@session.Role)</p>
}
else
{
    <p>Not logged in.</p>
}

@* <section class="py-5 mt-5" style="min-height: 90vh;">
    <div class="container text-center">
        @if (errorMessage != null)
        {
            <div class="alert alert-danger">@errorMessage</div>
        }

        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-4">
            @if (products == null && errorMessage == null)
            {
                <p>Načítám...</p>
            }
            else if (products?.Any() == true)
            {
                @foreach (var product in products)
                {
                    <div class="col">
                        <div class="card h-100 p-3">
                            <img src="img/produkty/@product.Photo"
                                 alt="@product.Nazev"
                                 class="card-img-top"
                                 style="height: 200px; object-fit: cover;" />
                            <div class="card-body text-start">
                                <h5 class="card-title">@product.Nazev</h5>
                                <p class="card-text">@product.Popis</p>
                                <p class="text-success fw-bold">@product.Cena.ToString("N0") Kč</p>
                            </div>
                            <div class="card-footer bg-transparent text-end">
                                <button class="btn btn-warning" @onclick="() => PridatDoKosiku(product.Id)">
                                    Přidat do košíku
                                </button>
                            </div>
                        </div>
                    </div>
                }
            }
            else if (products?.Any() == false)
            {
                <p>Žádné produkty k zobrazení</p>
            }
        </div>
    </div>
</section> *@


<section class="py-5 mt-5" style="display: flex; flex-direction: column; min-height: 90vh; margin: 0;">
    <div class="text-center">
        @if (products == null && errorMessage == null)
        {
            <p>Načítám...</p>
        }
        else if (products?.Any() == true)
        {       
            <div class="container">
                <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-4">
                    <!-- Produkty -->
                     @foreach (var product in products)
                    {
                        <div class="col" data-aos="fade-up">
                            <div class="card d-flex flex-column" style="min-height: 68vh; max-height: 68vh;padding: 1rem;">
                                <div class="card-body text-center">
                                    <!-- Obraz produktu -->
                                    <div>
                                        <img src="img/produkty/@product.Photo"
                                             alt="@product.Nazev"
                                             class="bd-placeholder-img card-img-top"
                                             style="width: auto; height: 200px; object-fit: cover;">
                                    </div>
                                    <!-- Jmeno produktu -->
                                    <div class="card-body">
                                        <h5 id="jmeno_produktu@product.Id_produkt" class="card-title text-start">
                                            @product.Nazev
                                        </h5>
                                        <p id="details@product.Id_produkt" class="card-text text-start">
                                            @product.Popis
                                        </p>
                                    </div>
                                </div>
                                <div class="align-bottom text-end">
                                    <!-- Cena produktu -->
                                    <p id="price@product.Id_produkt" class="cart-text" style="font-size: 1.5em; color: green;">
                                        @product.Cena Kč
                                    </p>

                                    <!-- Pridat do kosiku btn -->
                                    <input type="button" value="Přidat do košíku"
                                           class="btn btn-warning add-to-cart-btn" data-product-id="@product.Id">

                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        }
        else if (products?.Any() == false)
        {
            <p>Žádné produkty k zobrazení</p>
        }
    </div>
</section>

@code {
    private List<Product>? products;
    private string? errorMessage;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            products = await HomeService.GetAllProductsAsync();
        }
        catch (Exception ex)
        {
            errorMessage = "Chyba při načítání produktů: " + ex.Message;
        }
    }

    private void PridatDoKosiku(int productId)
    {
        // Logika pro přidání do košíku (budeš přidávat později)
    }
}

